= form_for @message, url: conversation_messages_path(@convo) do |f|
  .form-group
    = label_tag :subject, "Subject"
    = f.text_field :subject, class: 'form-control'
  .form-group
    = label_tag :body, "Body"
    = f.text_area :body, class: 'form-control'
  .form-group
    = f.submit "Create", class: 'btn btn-primary'