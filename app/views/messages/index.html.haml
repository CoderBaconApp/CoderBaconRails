%h1 Messages
- if @sent_messages.any?
  %h2 Sent Messages
  %table
    %tr
      %th Sender
      %th Receiver
      %th Subject
      %th Body
      %th Read
    = render partial: "message", collection: @sent_messages
